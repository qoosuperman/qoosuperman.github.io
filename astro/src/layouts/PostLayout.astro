---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  subtitle?: string;
  date: Date;
  updateDate?: Date;
  description?: string;
  tags?: string[];
  headerImg?: string;
  ogImage?: string;
}

const { title, subtitle, date, updateDate, description, tags, headerImg, ogImage } = Astro.props;

const fmt = (d: Date) =>
  d.toISOString().slice(0, 10);
---

<BaseLayout title={title} description={description ?? subtitle} ogImage={ogImage ?? headerImg}>
  <article>
    {headerImg && (
      <img
        src={headerImg}
        alt=""
        class="w-full h-64 object-cover rounded-lg mb-8"
        loading="eager"
      />
    )}
    <header class="mb-8">
      <h1 class="text-3xl font-bold leading-tight">{title}</h1>
      {subtitle && <p class="mt-2 text-neutral-600">{subtitle}</p>}
      <div class="mt-3 text-sm text-neutral-500 flex flex-wrap gap-x-4 gap-y-1">
        <time datetime={date.toISOString()}>{fmt(date)}</time>
        {updateDate && updateDate.getTime() !== date.getTime() && (
          <span>updated {fmt(updateDate)}</span>
        )}
        {tags && tags.length > 0 && (
          <span class="flex flex-wrap gap-2">
            {tags.map((t) => (
              <a href={`/tags/${t}/`} class="hover:text-neutral-900">#{t}</a>
            ))}
          </span>
        )}
      </div>
    </header>
    <div class="prose prose-neutral max-w-none">
      <slot />
    </div>
  </article>
</BaseLayout>
